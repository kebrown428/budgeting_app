# UI Layer - ViewModels

This package contains ViewModel classes that manage UI state and handle user interactions.
ViewModels communicate with the domain layer (use cases/repositories) and expose
state to the UI via StateFlow/Flow.
