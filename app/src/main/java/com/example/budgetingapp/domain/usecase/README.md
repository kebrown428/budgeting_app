# Domain Layer - Use Cases

This package contains use case classes that encapsulate business logic.
Each use case typically represents a single user action or business operation.
This keeps ViewModels thin and business logic testable and reusable.
