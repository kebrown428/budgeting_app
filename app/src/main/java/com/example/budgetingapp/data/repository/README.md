# Data Layer - Repository

This package contains repository implementations that abstract data sources.
Repositories provide a clean API for the domain layer to access data without
knowing where it comes from (database, network, etc.).
